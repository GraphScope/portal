{
  "scripts": {
    "prestart": "rm -rf package-lock.json && npm i",
    "check": "bash check-deploy.sh",
    "start": "npm run check &&  npm run status",
    "status": " npx pm2 status"
  },
  "dependencies": {
    "express": "^4.18.2",
    "http-proxy-middleware": "^2.0.6",
    "pm2": "^5.3.1"
  }
}
