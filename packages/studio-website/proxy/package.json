{
  "scripts": {
    "prestart": "rm -rf package-lock.json && npm i",
    "start": "npx pm2 start -f index.js && npx pm2 list"
  },
  "dependencies": {
    "express": "^4.18.2",
    "http-proxy-middleware": "^2.0.6",
    "pm2": "^5.3.1"
  }
}
